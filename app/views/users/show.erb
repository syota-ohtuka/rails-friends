<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>User Page</title>
</head>
<body>
  <h1>User page</h1>
  <p>
    <strong>name:</strong>
    <%= @show_user.name %>
  </p>
  <p>
    <strong>email:</strong>
    <%= @show_user.email %>
  </p>

  <%= link_to 'Back top', users_path %>
  | <%= link_to 'Edit', edit_user_path(@show_user) %>
</body>
</html>